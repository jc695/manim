[project]
name = "manim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "manim",
    "pycairo",
    "numpy",
    "pandas",
]
